import React from 'react';

const Uncle = ({house}) => {
    return (
        <div>
            <h2>It's me Uncle</h2>
            <p>house: {house}</p>
        </div>
    );
};

export default Uncle;